hydra:
  searchpath:
    - file://conf/global

defaults:
  - _self_
  - override hydra/job_logging: colorlog
  - override hydra/hydra_logging: colorlog
  - override hydra/launcher: custom_slurm

output_artifact_aliases: ["test"]

device: "cuda"
seed: 42