dataset_artifact_name: "MNIST_split_1:tmp"
batch_size: 128
epochs: 3000

lr: 1e-3
encoder:
  output_size: 128
  reading_block_cells: [128, 128]
  writing_block_cells: [128, 128]
  memory_size: 128
  processing_steps: 5
  dropout: 0.1
classifier:
  num_cells: [128, 128]

# defaults:
#   - override hydra/launcher: submitit_slurm
#
# hydra:
#   launcher:
#     timeout_min: 1440  # 24 hours
#
#     partition: alvis
#     account: NAISS2025-22-448
#
#     additional_parameters:
#       gres: gpu:T4:1
