trained_method_artifact_name: "train_shim2018-AFAContext_split_1-budget_5-seed_42:v2"
trained_classifier_artifact_name: "masked_mlp_classifier-AFAContext_split_1:v2"
seed: 42

defaults:
  - override hydra/launcher: submitit_slurm

hydra:
  launcher:
    timeout_min: 1440  # 24 hours

    partition: alvis
    account: NAISS2025-22-448

    additional_parameters:
      gres: gpu:T4:1
